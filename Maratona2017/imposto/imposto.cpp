#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);cin.tie(NULL);
    int N, C, A, B, C;
    vector<int> imposto;

    cin >> N >> C;

    for (int i=0; i<N; i++) {
        cin >> A;
        imposto.push_back(A);
    }

    for (int i=0; i<N-1; i++) {
        cin >> A >> B >> C;


    }

    return 0;
}
